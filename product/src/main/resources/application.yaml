spring:
  datasource:
    druid:
      driver-class-name: com.mysql.jdbc.Driver
      username: root
      password: 123456
      url: jdbc:mysql://101.35.249.158:3306/gulimall_pms?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
  application:
    name: gulimall-product
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
#sql映射文件
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
#  调整为自增主键
  global-config:
    db-config:
      id-type: auto
#     逻辑删除标志
      logic-delete-field: 1
      logic-not-delete-value: 0
server:
  port: 10000

#  打印日志
logging:
  level:
    com.tz.mall: debug